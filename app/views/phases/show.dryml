<show-page>
    <collection: replace>
  <table-plus with="&@tasks" fields="this, description, deadline, fee">
  	<prepend-header:>
      <div class="filter">
        Display by tasks: <filter-menu param-name="deadline" options="&Task.all"/>
      </div>
    </prepend-header:>
    <empty-message:>No phases ! :(</empty-message:>
  </table-plus>
  </collection:>
</show-page>